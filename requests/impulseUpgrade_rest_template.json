{
  "Subscriberid": {
    "system": "{{system}}",
    "subscriberIdentifier": "{{subscriberIdentifier}}"
  },
  "packageId": "{{packageId}}",
  "partNumber": "{{partNumber}}",
  "SerialNumber": "{{SerialNumber}}"
}
